[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "zlink"
version = "0.1.0"
dependencies = [
    "msgspec>=0.20.0",
]

[tool.hatch.build.targets.wheel]
packages = ["zlink"]
